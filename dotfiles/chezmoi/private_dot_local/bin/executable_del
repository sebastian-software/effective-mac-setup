#!/usr/bin/env bash
set -euo pipefail

if [[ -x /opt/homebrew/opt/trash/bin/trash ]]; then
  exec /opt/homebrew/opt/trash/bin/trash "$@"
fi

if command -v trash >/dev/null 2>&1; then
  exec trash "$@"
fi

echo "trash CLI is not installed. Run: brew bundle --file Brewfile" >&2
exit 127
